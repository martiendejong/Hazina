<Window x:Class="Hazina.TaskRunner.UI.TaskManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Hazina.TaskRunner.UI"
        mc:Ignorable="d"
        Title="Hazina Task Manager" Height="600" Width="900"
        WindowStartupLocation="CenterScreen"
        MinWidth="700" MinHeight="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <ToolBarTray Grid.Row="0" Background="WhiteSmoke">
            <ToolBar>
                <Button Name="BtnAdd" Click="BtnAdd_Click" Padding="10,5">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="+ " FontWeight="Bold"/>
                        <TextBlock Text="Add Task"/>
                    </StackPanel>
                </Button>
                <Button Name="BtnEdit" Click="BtnEdit_Click" Padding="10,5" IsEnabled="{Binding ElementName=TasksGrid, Path=SelectedItem, Converter={StaticResource NullToBoolConverter}}">
                    <TextBlock Text="Edit"/>
                </Button>
                <Button Name="BtnDelete" Click="BtnDelete_Click" Padding="10,5" IsEnabled="{Binding ElementName=TasksGrid, Path=SelectedItem, Converter={StaticResource NullToBoolConverter}}">
                    <TextBlock Text="Delete"/>
                </Button>
                <Separator/>
                <Button Name="BtnRunNow" Click="BtnRunNow_Click" Padding="10,5" IsEnabled="{Binding ElementName=TasksGrid, Path=SelectedItem, Converter={StaticResource NullToBoolConverter}}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="▶ " Foreground="Green" FontWeight="Bold"/>
                        <TextBlock Text="Run Now"/>
                    </StackPanel>
                </Button>
                <Separator/>
                <Button Name="BtnRefresh" Click="BtnRefresh_Click" Padding="10,5">
                    <TextBlock Text="Refresh"/>
                </Button>
            </ToolBar>
        </ToolBarTray>

        <!-- Task List -->
        <DataGrid Name="TasksGrid" Grid.Row="1"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  SelectionMode="Single"
                  AlternatingRowBackground="WhiteSmoke"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" MinWidth="150"/>
                <DataGridTextColumn Header="Cron Expression" Binding="{Binding CronExpression}" Width="150"/>
                <DataGridTextColumn Header="Next Run" Binding="{Binding NextRun, StringFormat='yyyy-MM-dd HH:mm'}" Width="150"/>
                <DataGridTextColumn Header="Last Run" Binding="{Binding LastRun, StringFormat='yyyy-MM-dd HH:mm'}" Width="150"/>
                <DataGridCheckBoxColumn Header="Enabled" Binding="{Binding Enabled}" Width="80"/>
                <DataGridCheckBoxColumn Header="Elevated" Binding="{Binding RunElevated}" Width="80"/>
                <DataGridTextColumn Header="Timeout (s)" Binding="{Binding TimeoutSeconds}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Status Bar -->
        <StatusBar Grid.Row="2">
            <StatusBarItem>
                <TextBlock Name="StatusText" Text="Ready"/>
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <TextBlock Name="TaskCountText" Text="0 tasks"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
