<Window x:Class="Hazina.TaskRunner.UI.TaskEditorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Hazina.TaskRunner.UI"
        mc:Ignorable="d"
        Title="Task Editor" Height="500" Width="550"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Name -->
        <Label Grid.Row="0" Content="Task Name:" FontWeight="Bold"/>
        <TextBox Grid.Row="1" Name="TxtName" Margin="0,0,0,10" Padding="5"/>

        <!-- Script Path -->
        <Label Grid.Row="2" Content="Script Path:" FontWeight="Bold"/>
        <Grid Grid.Row="3" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Name="TxtScriptPath" Grid.Column="0" Padding="5" Margin="0,0,5,0"/>
            <Button Name="BtnBrowse" Grid.Column="1" Content="Browse..." Click="BtnBrowse_Click" Padding="10,5"/>
        </Grid>

        <!-- Cron Expression -->
        <Label Grid.Row="4" Content="Cron Expression:" FontWeight="Bold"/>
        <Grid Grid.Row="5" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Name="TxtCronExpression" Grid.Column="0" Padding="5" Margin="0,0,5,0" TextChanged="TxtCronExpression_TextChanged"/>
            <TextBlock Name="TxtCronPreview" Grid.Column="1" VerticalAlignment="Center" Foreground="Gray" Margin="10,0,0,0"/>
        </Grid>

        <!-- Options -->
        <Label Grid.Row="6" Content="Options:" FontWeight="Bold"/>
        <StackPanel Grid.Row="7" Orientation="Vertical" Margin="0,0,0,10">
            <CheckBox Name="ChkEnabled" Content="Enabled" Margin="0,5"/>
            <CheckBox Name="ChkElevated" Content="Run Elevated (Administrator)" Margin="0,5"/>
            <Grid Margin="0,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Timeout (seconds):" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBox Name="TxtTimeout" Grid.Column="1" Padding="5" Text="300"/>
            </Grid>
        </StackPanel>

        <!-- Helper Text -->
        <TextBlock Grid.Row="8" TextWrapping="Wrap" FontSize="11" Foreground="Gray">
            <Run Text="Cron expression format: minute hour day month weekday"/><LineBreak/>
            <Run Text="Examples: '0 6 * * *' (daily at 6am), '*/5 * * * *' (every 5 minutes)"/><LineBreak/>
            <Run Text="Or with seconds: '0 */5 * * * *' (every 5 minutes with seconds)"/>
        </TextBlock>

        <!-- Buttons -->
        <StackPanel Grid.Row="9" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Name="BtnOK" Content="OK" Click="BtnOK_Click" Padding="20,8" Margin="0,0,10,0" IsDefault="True"/>
            <Button Name="BtnCancel" Content="Cancel" Click="BtnCancel_Click" Padding="20,8" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
